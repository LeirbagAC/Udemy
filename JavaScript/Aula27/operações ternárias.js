// (condição) ? 'Valor verdadeiro': 'Valor falso'
const portuacaoUsuario = 999;
const nivelUsuario = portuacaoUsuario >= 1000 ? 'Usuário VIP': 'Usuário Normal';
console.log(nivelUsuario);

const idade = 15;
const idadeMilitar = idade >= 18 ? 'Deve se alistar': 'Não pode'; 
console.log(idadeMilitar);

const idadeVotar = idade >= 16 ? 'Pode votar' : 'Não pode'
console.log(idadeVotar);
